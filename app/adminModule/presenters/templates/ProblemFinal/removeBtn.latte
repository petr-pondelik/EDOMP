{if !$item->is_used}
    <a href={plink ProblemFinal:default do => delete, problem_id => $item->problem_id} title="Odstranit" class="btn btn-danger btn-sm ajax">
        <span class="fa fa-trash"></span>
    </a>
{else}
    <a href="#" class="btn btn-danger btn-sm disabled" title="Úloha je využívána.">
        <span class="fa fa-trash"></span>
    </a>
{/if}